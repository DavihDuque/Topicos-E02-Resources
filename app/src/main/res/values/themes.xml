<resources xmlns:tools="http://schemas.android.com/tools">
<!-- Paleta Base -->
<style name="Theme.Resources" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
    <!-- Cor primária -->
    <item name="colorPrimary">@color/gold_primary</item>
    <item name="colorPrimaryDark">@color/gold_primary_dark</item>
    <item name="colorAccent">@color/gold_accent</item>

    <!-- Plano de Fundo e Texto -->
    <item name="android:textColor">@color/gold_text</item>
    <item name="android:colorBackground">@color/gold_background</item>
</style>
</resources>
